import { TagsProps } from "cdm/ComponentsModel";
import React from "react";

const TagsPortal = (tagsProps: TagsProps) => {
  const { intialState } = tagsProps;
  return <span></span>;
};
export default TagsPortal;
